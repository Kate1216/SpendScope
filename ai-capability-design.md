# SpendScope AI 能力设计文档

## 1. AI 能力定位

SpendScope 的 AI 能力目标不是简单地“把完整账单发给大模型生成总结”，而是围绕账单分析中的真实难点，设计一个安全、可控、可反馈的 AI 辅助分类与洞察系统。

当前版本以本地规则为主，后续逐步引入 AI 能力。

核心原则：

- 本地规则优先处理明确交易
- 用户手动修正形成分类记忆
- AI 只处理不确定交易
- 用户确认后再沉淀为长期规则
- 尽量不上传完整账单明细
- 明确保护用户隐私

---

## 2. 当前分类方案

当前 SpendScope 采用本地规则分类。

### 2.1 分类输入字段

系统主要根据以下字段判断类别：

- 平台 platform
- 交易对象 merchant
- 商品说明 description
- 交易类型 transactionType
- 收支类型 type
- 金额 amount

---

### 2.2 分类逻辑

当前分类逻辑可以概括为：

```text
账单解析
↓
统一字段
↓
判断收支类型
↓
识别退款、补偿、账户转移、排除项
↓
基于关键词判断消费类别
↓
无法判断则归为“其他”
